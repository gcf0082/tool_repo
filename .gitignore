/dist/
/tool_repo
/tool_cli
*.test
# Note: tool_cli_bin/ contains committed empty placeholders so plain
# 'go build' works. dist.sh and test.sh overwrite them with real
# cross-compiled tool_cli binaries before deploying.
